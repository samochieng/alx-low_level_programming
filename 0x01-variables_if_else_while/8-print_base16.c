#include <stdio.h>
#include <stdlib.h>
/**
*main - print all hexadecimal numbers
*
* Description: prints all the numbers of base 16 in lowercase
* Return: 0;
*/
int main(void)
{
	int ch;
	int k;

	for (ch = 0; ch <= 9; ch++)
	{
		putchar(ch);
	}
	if (k = 'A'); (k <= 'F'); (k++)
	{
		putchar(k);
	}
	else
	{
	}
	putchar('\n');
	return (0);
}

