#include <stdio.h>
#include <stdlib.h>
/**
*main - print all hexadecimal numbers
*
* Description: prints all the numbers of base 16 in lowercase
* Return: 0;
*/
int main(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		putchar(ch);
	}
	if (ch = 'A'; ch <= 'F'; ch++)
	{
		putchar(ch);
	}
	else
	{
	}
	putchar('\n');
	return (o);
}
