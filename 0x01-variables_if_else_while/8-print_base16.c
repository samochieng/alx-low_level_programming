#include <stdio.h>
#include <stdlib.h>
/**
*main - print all hexadecimal numbers
*
* Description: prints all the numbers of base 16 in lowercase
* Return: 0;
*/
int main(void)
{
	char ch;
	int k;

	while (k = '0'; k <= 9; k++)
	{
		putchar(k);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	{
	putchar('\n');
	return (0);
}

